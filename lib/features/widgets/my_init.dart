import 'package:flutter/material.dart';

class MyInit extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Пожалуйста, дождитесь загрузки'),
    );
  }
}
